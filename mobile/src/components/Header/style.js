import { StyleSheet } from 'react-native';

const style = StyleSheet.create({
    header:{
        flexDirection: "row",
        alignItems: "center"
    },
    fotoUsuario: {
        width:  50,
        height: 50,
        margin: 10,
        borderRadius: 30
    }
});

export default style;